version: incident/v1
id: hl35ck73pm83
name: fly ssh console returns error 500
impact: none
systems: []
timeline:
    - ts: "2026-05-15 16:29:20"
      description: |-
        A problem with our  vault used to issue temporary certificates for SSH sessions is causing calls to `fly ssh console` and `fly console` to return error 500.

        Our team has identified the cause and is deploying a fix.
    - ts: "2026-05-15 16:40:13"
      description: We've deployed a fix and are monitoring as error rates normalize. `fly ssh console` should be working now.
